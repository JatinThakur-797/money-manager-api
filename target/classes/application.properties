# ===================================================================
# M O N E Y M A N A G E R  -  M A I N  C O N F I G U R A T I O N
# ===================================================================

# --- General Application Settings ---
spring.application.name=moneymanager

server.port=${SERVER_PORT:8080}
server.servlet.context-path=/api/v1.0

spring.datasource.url=jdbc:mysql://localhost:3306/money_manager
spring.datasource.username=root
spring.datasource.password=Jatin@999

# --- JPA/Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Email Configuration (reads from environment variables) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${SPRING_MAIL_USERNAME}

# --- Custom Application Properties (reads from environment variables) ---
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL:http://localhost:3000}
money.manager.backend.url=${MONEY_MANAGER_BACKEND_URL:http://localhost:8080}
jwt.secret=${JWT_SECRET:sdfsdfsafdsajdskfklsaklfskcnskjafewfwfkfdaskljfias3}

spring.profiles.active=prod


